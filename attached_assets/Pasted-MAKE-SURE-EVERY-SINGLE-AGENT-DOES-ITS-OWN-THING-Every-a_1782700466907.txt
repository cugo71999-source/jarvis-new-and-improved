MAKE SURE EVERY SINGLE AGENT DOES ITS OWN THING. Every agent must have a clearly defined responsibility with no overlapping functionality. JARVIS acts only as the master orchestrator and never performs specialized tasks itself. Instead, JARVIS analyzes the user's request, creates an execution plan, delegates work to the appropriate agents, waits for their responses, combines the results, and returns a polished final answer. Every agent must be able to operate independently and expose its own API so JARVIS can call it at any time. Agents must also be capable of calling other agents, but ONLY when absolutely necessary to complete their own assigned task. They should never duplicate work or bypass the orchestration system. Build a proper inter-agent communication framework with task queues, structured messaging, retries, health monitoring, execution logging, and dependency management so the architecture is modular, scalable, and maintainable.

Completely redesign and elevate the entire UI to feel like a premium desktop operating system rather than a typical AI chatbot. Every animation, transition, blur effect, panel, and interaction should feel polished and modern. The center JARVIS orb should become significantly more advanced. Remove any unnecessary floating decorative orbs and instead make the primary orb feel alive using procedural animations, layered energy effects, dynamic lighting, volumetric glow, glass-like reflections, particle systems, subtle breathing motion, ripple effects, voice-reactive pulses, thinking animations, listening animations, speaking animations, success animations, error animations, and sleep/wake transitions. The orb alone should visually communicate the current state of the AI.

Improve every panel throughout the interface. The side panels should be fully animated, dockable, collapsible, resizable, and built with a premium glassmorphism design. Add smoother transitions, better typography, improved spacing, richer icons, live widgets, task monitoring, agent activity displays, system statistics, conversation history, memory inspection, plugin management, downloads, notifications, and quick actions while maintaining an elegant and uncluttered appearance.

Completely fix the microphone system. Speaking into the microphone should immediately detect voice input reliably. Implement proper microphone initialization, continuous input detection, voice activity detection, silence detection, background noise filtering, echo cancellation, automatic gain control, and automatic recovery if the selected device becomes unavailable. Display a real-time microphone level indicator so users can confirm that audio is being detected correctly.

Create a dedicated Settings page with a polished interface that includes categories for General, Appearance, AI Models, Voice, Microphone, Speakers, Wake Word, Memory, Plugins, Automation, Security, Performance, and Advanced options. Under the Microphone section, automatically detect every available recording device connected to the computer and present them in a dropdown list so the user can choose which microphone JARVIS should use. Save the selected device and automatically switch to it whenever the application starts.

Add a fully functional wake word system. When the user says "Hey Jarvis", the application should automatically wake up, begin listening, and process the following command without requiring any button presses. The wake word engine should continuously listen using minimal system resources, ignore false positives as much as possible, support configurable sensitivity, and expose settings to enable, disable, or customize the wake phrase in the future.

Create a comprehensive Instructions tab inside the application that documents every feature, every agent, the overall architecture, keyboard shortcuts, voice controls, plugin system, automation capabilities, troubleshooting steps, installation details, configuration options, privacy information, and best practices. This documentation should update automatically as new features are added.

Build a professional Windows installer (.exe) that requires virtually no manual setup from the user. When the installer is launched, it should automatically detect the operating system, verify prerequisites, install every required dependency, configure the environment, download all required runtimes, AI models, libraries, frameworks, and supporting components needed for the project to function correctly. The installer should automatically install everything necessary without requiring the user to open a terminal or manually execute commands. After each component is downloaded, verify its integrity before continuing.

The installer should feature a polished, premium installation interface with smooth animations, installation progress, download speeds, estimated remaining time, detailed logs, dependency status indicators, and branded visuals consistent with the JARVIS aesthetic. If any dependency already exists, intelligently skip or update it instead of reinstalling it. If a download fails, automatically retry and provide meaningful recovery options.

After confirming that every dependency, runtime, model, and required component has been successfully installed and configured, the installer should automatically launch the JARVIS application and perform a first-run initialization that verifies all systems are functioning correctly. The goal is that a brand-new user can run a single installer, wait for it to finish, and immediately begin using JARVIS without manually installing anything else.

Throughout the entire project, prioritize modular architecture, maintainability, scalability, performance, and reliability. Every subsystem should be independently testable, loosely coupled, and designed for future expansion so new agents, plugins, AI models, and capabilities can be added with minimal changes to the existing codebase. The finished application should feel like a complete AI operating system rather than a standard desktop chatbot.